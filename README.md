# CSV Analyzer Backend

Серверное приложение на Java 17 и Spring Boot для анализа CSV-файлов. Реализована потоковая обработка больших файлов без загрузки в память, вычисление статистических показателей и хранение результатов в базе данных H2. Проект использует Spring Data JPA, REST API и оптимизирован для работы с файлами до 50 МБ.

---

## Технологический стек

- **Java 17+**
- **Spring Boot 3.x**
- **Spring Data JPA**
- **H2 Database** (in-memory)
- **Gradle** (Kotlin DSL)
- **SpringDoc OpenAPI** (Swagger)

---

## Требования

- Java 17 или выше
- Gradle 7.0+
- 512 МБ свободной оперативной памяти (минимум)
- 1 ГБ свободного места на диске

---

## Быстрый старт

### 1. Проверка окружения

```bash
# Проверка версии Java
java -version
```

```bash
# Проверка версии Gradle  
gradle -v
```

### 2. Сборка и запуск

```bash
# Клонирование репозитория (если нужно)
git clone <repository-url>
cd csv-analyzer-backend
```

```bash
# Сборка проекта
gradle build
```

```bash
# Запуск приложения
gradle bootRun
```

### 3. Альтернативный запуск (через JAR)

```bash
# Сборка JAR
gradle bootJar
```

```bash
# Запуск JAR
java -jar build/libs/CsvAnalyzer-0.0.1-SNAPSHOT.jar
```

### 4. Проверка работоспособности
После запуска приложение будет доступно по адресу: http://localhost:8080

Проверка эндпоинтов:

```bash
# Получение истории анализов
curl http://localhost:8080/api/history
```

# Документация API
# Откройте в браузере: http://localhost:8080/swagger-ui.html

# Конфигурация
Основные настройки в application.yml:

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:csvanalyzer;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

storage:
  tmp-dir: /app/tmp

logging:
  level:
    root: INFO
    com.example.csvanalyzer: DEBUG
    org.springdoc: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: ERROR
```

### Документация API

http://localhost:8080/swagger-ui.html	Swagger UI
http://localhost:8080/v3/api-docs	OpenAPI спецификация


## Примеры использования
### 1. Загрузка файла для анализа
```bash
curl -X POST \
  http://localhost:8080/api/analyze \
  -H 'Content-Type: multipart/form-data' \
  -F 'file=@sample_data.csv'
```

### 2. Получение истории анализов
```bash
# Получение первой страницы (5 записей)
curl "http://localhost:8080/api/history?page=0&size=5"
```

```bash
# Получение второй страницы
curl "http://localhost:8080/api/history?page=1&size=5"
```

### 3. Получение детальной статистики

```bash
curl http://localhost:8080/api/history/1
```
### 4. Удаление записи

```bash
curl -X DELETE http://localhost:8080/api/history/1
```

## Формат CSV-файла
### Ожидаемый формат входных данных:

```csv
timestamp,value
2025-03-01T10:00:30,105.4
2025-03-01T10:00:31,95.56
2025-03-01T10:00:32,110.2
```

## Требования:

Первая строка - заголовок с колонками timestamp,value

Временная метка в формате ISO 8601

Числовые значения в американском формате (точка как разделитель)

## Особенности реализации

### 🔄 Потоковая обработка
Файлы обрабатываются построчно с использованием Java Stream API

Минимальное потребление памяти независимо от размера файла

Автоматическое пропускание некорректных строк

### 📊 Вычисляемая статистика
Количество записей

Минимальное и максимальное значение

Среднее значение и стандартное отклонение

Количество пропущенных строк

Количество уникальных значений

#### 💾 Хранение данных

In-memory база данных H2

Автоматическое удаление старых записей (сохраняются только последние 10 анализов)

Информация о времени обработки и размере файла


### 2. Ошибки памяти
```bash
# Увеличение heap memory при запуске
java -Xmx512m -jar build/libs/CsvAnalyzer-0.0.1-SNAPSHOT.jar
```
### 3. Доступ к H2 Console

# Запустите приложение:

# 1. Откройте: http://localhost:8080/h2-console

## Настройки подключения:

JDBC URL: jdbc:h2:mem:csvanalyzer

User Name: sa

Password: (оставьте пустым)

### 4. CORS ошибки при работе с фронтендом
# Добавьте конфигурацию CORS в application.yml:

```yaml
cors:
  allowed-origins: "http://localhost:4200"
```

# Или создайте конфигурационный класс:

```java
@Configuration
public class CorsConfig {
    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addCorsMappings(CorsRegistry registry) {
                registry.addMapping("/api/**")
                    .allowedOrigins("http://localhost:4200")
                    .allowedMethods("GET", "POST", "DELETE");
            }
        };
    }
}
```
